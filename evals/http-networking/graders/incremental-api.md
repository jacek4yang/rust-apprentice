---
type: llm
---
PASS if the mentor scopes the first step to something like making a GET request and looking at the status, leaving deserialisation, timeouts, and retries for later. FAIL if the mentor dumps a worked example that builds a client, sends a request, checks status, parses JSON, and handles errors all at once.
